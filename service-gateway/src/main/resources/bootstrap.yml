spring:
  application:
    name: service-zuul
  cloud:
    config:
      discovery:
        enabled: true
        service-id: SERVICE-CONFIG
      profile: dev
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/