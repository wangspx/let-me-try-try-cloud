spring:
  application:
    name: service-db
  cloud:
    config:
      discovery:
        enabled: true
        service-id: ENVIRONMENT-CONFIGURATION-CENTER
      profile: dev
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
logging:
  level:
    org.springframework.cloud.openfeign: debug