spring:
  application:
    name: service-summer
  cloud:
    config:
      discovery:
        enabled: true
        service-id: SERVICE-CONFIG
      profile: dev
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/,http://localhost:8762/eureka/